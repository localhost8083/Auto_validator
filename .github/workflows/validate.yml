name: Validate M3U playlists

on:
  schedule:
    # Every 3 hours (UTC). Change to "0 */2 * * *" for every 2 hours.
    - cron: "0 */3 * * *"
  workflow_dispatch: {}
  push:
    branches: [main]
    paths:
      - "sources.txt"
      - "validator.py"
      - "checker.py"
      - "m3u.py"
      - ".github/workflows/validate.yml"

# Allow the workflow to push the refreshed outputs back to the repo.
permissions:
  contents: write

concurrency:
  group: validate-m3u
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install ffmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          ffmpeg -version | head -n 1

      - name: Run validator
        env:
          CONCURRENCY: "20"
          TEST_WORKERS: "8"
          DEAD_TIMEOUT: "9000"
          TEST_TIMEOUT: "12000"
          DEEP_TEST: "1"
          # Stage 3: ffmpeg actually decodes a few seconds of each surviving
          # channel for the strictest validation. Set FFPROBE_TEST=0 to disable.
          FFPROBE_TEST: "1"
          FFPROBE_WORKERS: "6"
          FFPROBE_SECS: "4"
          FFPROBE_TIMEOUT: "15000"
        run: python validator.py

      - name: Commit refreshed outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add output/
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore: refresh validated playlists ($(date -u '+%Y-%m-%d %H:%M UTC'))"
            git pull --rebase --autostash origin main
            git push
          fi
